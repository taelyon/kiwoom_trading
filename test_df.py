import sqlite3, pandas as pd; from strategy_utils import prepare_buy_strategy_locals; conn = sqlite3.connect('data/stock_data.db'); df = pd.read_sql_query(\"SELECT * FROM stock_data WHERE datetime >= '2026-06-11' LIMIT 10\", conn); api_df = df.rename(columns={'tic_open': 'open', 'tic_high': 'high', 'tic_low': 'low', 'tic_close': 'close', 'tic_volume': 'volume', 'tic_strength': 'strength'}); print(api_df['volume'].__class__); prepare_buy_strategy_locals(api_df['code'].iloc[0], api_df, pd.DataFrame(), None)
